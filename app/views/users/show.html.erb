<h1>Name: <%=@user.username %></h1>
<% if admin? %>
    <h2>Admin</h2>
<% end %>

<p><%= link_to 'See all documents', user_documents_path(@user) %></p>
<p><%= link_to 'Add a new document', new_user_document_path(@user) %></p>

