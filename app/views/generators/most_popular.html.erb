<p class="title is-5">Most popular generators</p>
<div class="column">
    <div class="tile is-ancestor">
        <div class="tile is-12 is-vertical is-parent">
            <% @generator_popularity.each do |key, value| %>
                <article class="tile is-child notification is-link">
                    <%= link_to @generators.where("id = #{key}")[0].name, generator_path(key), :class=>"subtitle is-6" %>
                    (<%= value %> documents)
                </article>
            <% end %>
        </div>
    </div>
</div>
<br>
<p class="subtitle is-6">
    <span class="tag is-primary"><%= link_to "Create new generator", new_generator_path, :class=>"has-text-white" %></span>
</p>